# GENERATED from langgraph-agent/cocoon_agent/api/schemas.py by scripts/export_openapi.py.
# Do not edit by hand. See API_CONTRACT.md for the change process.
openapi: 3.1.0
info:
  title: Cocoon backend API
  summary: LangGraph application backend for the Cocoon voice assistant (Team Butterfly).
  version: 1.0.0
paths:
  /healthz:
    get:
      tags:
      - health
      summary: Healthz
      operationId: healthz_healthz_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
  /readyz:
    get:
      tags:
      - health
      summary: Readyz
      operationId: readyz_readyz_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReadyResponse'
        '503':
          description: Not ready
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReadyResponse'
  /v1/me:
    get:
      tags:
      - identity
      summary: Me
      operationId: me_v1_me_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MeResponse'
        '401':
          description: 'unauthorized: missing, malformed, unknown, expired or revoked bearer token, or more than
            one Authorization header (the cause is deliberately not distinguished)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: 'forbidden: authenticated, but this role may not use the route'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Unknown session or resource; also returned for a session an operator does not own
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Malformed request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Unexpected server error (retryable)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          description: 'auth_unavailable: the token store could not be read (retryable; never treated as success)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      security:
      - HTTPBearer: []
  /v1/sessions:
    post:
      tags:
      - sessions
      summary: Create Session
      operationId: create_session_v1_sessions_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SessionCreateRequest'
        required: true
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Session'
        '401':
          description: 'unauthorized: missing, malformed, unknown, expired or revoked bearer token, or more than
            one Authorization header (the cause is deliberately not distinguished)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: 'forbidden: authenticated, but this role may not use the route'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Unknown session or resource; also returned for a session an operator does not own
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Malformed request (validation_error), or a NEW session whose machine_id (unknown_machine,
            checked first) or operator_id (unknown_operator) is not in the verified catalog, or whose site/shift
            has no trusted binding
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Unexpected server error (retryable)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          description: 'catalog_unavailable: no verified catalog loaded; new sessions refused; or auth_unavailable:
            the token store could not be read (retryable; never treated as success)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '200':
          description: Existing session for this client_session_key
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Session'
        '409':
          description: Key already bound to a different room/participant/operator/machine/site/shift
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
      security:
      - HTTPBearer: []
  /v1/sessions/{session_id}/turns:
    post:
      tags:
      - turns
      summary: Submit Turn
      operationId: submit_turn_v1_sessions__session_id__turns_post
      security:
      - HTTPBearer: []
      parameters:
      - name: session_id
        in: path
        required: true
        schema:
          type: string
          title: Session Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TurnRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TurnResult'
        '401':
          description: 'unauthorized: missing, malformed, unknown, expired or revoked bearer token, or more than
            one Authorization header (the cause is deliberately not distinguished)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: 'forbidden: authenticated, but this role may not use the route'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Unknown session or resource; also returned for a session an operator does not own
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Malformed request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Unexpected server error (retryable)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          description: 'LLM unavailable or turn timed out (retryable, same turn_id); or auth_unavailable: the token
            store could not be read (retryable; never treated as success)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '202':
          description: Same turn_id is still processing. Wait retry_after_ms (also the Retry-After header, seconds)
            then GET poll_url (also the Location header). Do not resubmit with a new turn_id.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TurnResult'
        '409':
          description: turn_id reused with a different text or source
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/sessions/{session_id}/turns/{turn_id}:
    get:
      tags:
      - turns
      summary: Get Turn
      operationId: get_turn_v1_sessions__session_id__turns__turn_id__get
      security:
      - HTTPBearer: []
      parameters:
      - name: session_id
        in: path
        required: true
        schema:
          type: string
          title: Session Id
      - name: turn_id
        in: path
        required: true
        schema:
          type: string
          title: Turn Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TurnResult'
        '401':
          description: 'unauthorized: missing, malformed, unknown, expired or revoked bearer token, or more than
            one Authorization header (the cause is deliberately not distinguished)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: 'forbidden: authenticated, but this role may not use the route'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Unknown session or resource; also returned for a session an operator does not own
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Malformed request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Unexpected server error (retryable)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          description: 'auth_unavailable: the token store could not be read (retryable; never treated as success)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/sessions/{session_id}/commands:
    post:
      tags:
      - commands
      summary: Submit Command
      operationId: submit_command_v1_sessions__session_id__commands_post
      security:
      - HTTPBearer: []
      parameters:
      - name: session_id
        in: path
        required: true
        schema:
          type: string
          title: Session Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SessionCommand'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionCommandResult'
        '401':
          description: 'unauthorized: missing, malformed, unknown, expired or revoked bearer token, or more than
            one Authorization header (the cause is deliberately not distinguished)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: 'forbidden: authenticated, but this role may not use the route'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Unknown session or resource; also returned for a session an operator does not own
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Malformed request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Unexpected server error (retryable)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          description: 'auth_unavailable: the token store could not be read (retryable; never treated as success)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: idempotency_conflict (command_id reused with a different payload), version_conflict (expected_version
            is stale; details give the current version) or invalid_transition (the task is not in a state this command
            accepts)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/sessions/{session_id}/commands/{command_id}:
    get:
      tags:
      - commands
      summary: Get Command
      operationId: get_command_v1_sessions__session_id__commands__command_id__get
      security:
      - HTTPBearer: []
      parameters:
      - name: session_id
        in: path
        required: true
        schema:
          type: string
          title: Session Id
      - name: command_id
        in: path
        required: true
        schema:
          type: string
          title: Command Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionCommandResult'
        '401':
          description: 'unauthorized: missing, malformed, unknown, expired or revoked bearer token, or more than
            one Authorization header (the cause is deliberately not distinguished)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: 'forbidden: authenticated, but this role may not use the route'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Unknown session or resource; also returned for a session an operator does not own
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Malformed request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Unexpected server error (retryable)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          description: 'auth_unavailable: the token store could not be read (retryable; never treated as success)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/sessions/{session_id}/state:
    get:
      tags:
      - state
      summary: Get State
      operationId: get_state_v1_sessions__session_id__state_get
      security:
      - HTTPBearer: []
      parameters:
      - name: session_id
        in: path
        required: true
        schema:
          type: string
          title: Session Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionState'
        '401':
          description: 'unauthorized: missing, malformed, unknown, expired or revoked bearer token, or more than
            one Authorization header (the cause is deliberately not distinguished)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: 'forbidden: authenticated, but this role may not use the route'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Unknown session or resource; also returned for a session an operator does not own
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Malformed request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Unexpected server error (retryable)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          description: 'auth_unavailable: the token store could not be read (retryable; never treated as success)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/sessions/{session_id}/telemetry:
    post:
      tags:
      - telemetry
      summary: Submit Telemetry
      operationId: submit_telemetry_v1_sessions__session_id__telemetry_post
      security:
      - HTTPBearer: []
      parameters:
      - name: session_id
        in: path
        required: true
        schema:
          type: string
          title: Session Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TelemetryRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TelemetryResult'
        '401':
          description: 'unauthorized: missing, malformed, unknown, expired or revoked bearer token, or more than
            one Authorization header (the cause is deliberately not distinguished)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: 'forbidden: authenticated, but this role may not use the route'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Unknown session or resource; also returned for a session an operator does not own
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Malformed request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Unexpected server error (retryable)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          description: 'auth_unavailable: the token store could not be read (retryable; never treated as success)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: event_id reused with a different payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/sessions/{session_id}/detections:
    post:
      tags:
      - telemetry
      summary: Submit Detection
      description: 'One lifecycle step of an alert episode found by the trusted external detector (simulated data
        only).

        open/escalated announce, resolved clears, update and info are recorded only. Needs a prior telemetry sample

        in the session (422 machine_state_unavailable).'
      operationId: submit_detection_v1_sessions__session_id__detections_post
      security:
      - HTTPBearer: []
      parameters:
      - name: session_id
        in: path
        required: true
        schema:
          type: string
          title: Session Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DetectionRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetectionResult'
        '401':
          description: 'unauthorized: missing, malformed, unknown, expired or revoked bearer token, or more than
            one Authorization header (the cause is deliberately not distinguished)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: 'forbidden: authenticated, but this role may not use the route'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Unknown session or resource; also returned for a session an operator does not own
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Malformed request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Unexpected server error (retryable)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          description: 'auth_unavailable: the token store could not be read (retryable; never treated as success)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: detection_id reused with a different payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/sessions/{session_id}/events:
    get:
      tags:
      - announcements
      summary: List Events
      operationId: list_events_v1_sessions__session_id__events_get
      security:
      - HTTPBearer: []
      parameters:
      - name: session_id
        in: path
        required: true
        schema:
          type: string
          title: Session Id
      - name: after
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          description: Return announcements with sequence > after
          default: 0
          title: After
        description: Return announcements with sequence > after
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 100
          minimum: 1
          default: 20
          title: Limit
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EventsPage'
        '401':
          description: 'unauthorized: missing, malformed, unknown, expired or revoked bearer token, or more than
            one Authorization header (the cause is deliberately not distinguished)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: 'forbidden: authenticated, but this role may not use the route'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Unknown session or resource; also returned for a session an operator does not own
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Malformed request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Unexpected server error (retryable)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          description: 'auth_unavailable: the token store could not be read (retryable; never treated as success)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /v1/sessions/{session_id}/events/{event_id}/delivery:
    post:
      tags:
      - announcements
      summary: Record Delivery
      operationId: record_delivery_v1_sessions__session_id__events__event_id__delivery_post
      security:
      - HTTPBearer: []
      parameters:
      - name: session_id
        in: path
        required: true
        schema:
          type: string
          title: Session Id
      - name: event_id
        in: path
        required: true
        schema:
          type: string
          title: Event Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeliveryReport'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeliveryRecord'
        '401':
          description: 'unauthorized: missing, malformed, unknown, expired or revoked bearer token, or more than
            one Authorization header (the cause is deliberately not distinguished)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: 'forbidden: authenticated, but this role may not use the route'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Unknown session or resource; also returned for a session an operator does not own
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: Malformed request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Unexpected server error (retryable)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          description: 'auth_unavailable: the token store could not be read (retryable; never treated as success)'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
components:
  schemas:
    ActionRecord:
      properties:
        action_id:
          type: string
          title: Action Id
        kind:
          type: string
          title: Kind
        outcome:
          type: string
          enum:
          - completed
          - failed
          - unknown
          title: Outcome
        record_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Record Type
        record_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Record Id
        summary:
          type: string
          title: Summary
        state_version:
          anyOf:
          - type: integer
          - type: 'null'
          title: State Version
        created_at:
          type: string
          format: date-time
          title: Created At
      additionalProperties: false
      type: object
      required:
      - action_id
      - kind
      - outcome
      - summary
      - created_at
      title: ActionRecord
      description: Durable outcome of one write in this turn (committed together with the write).
    Alert:
      properties:
        alert_id:
          type: string
          title: Alert Id
        rule_id:
          type: string
          title: Rule Id
        alert_type:
          type: string
          pattern: ^[a-z][a-z0-9_]{1,63}$
          title: Alert Type
          description: 'Policy rules: seatbelt_unfastened, prolonged_idle or idle_unbelted. Detector alerts: the
            detection type in lower case, e.g. proximity_danger_zone. Treat unknown values as opaque.'
        source:
          type: string
          enum:
          - policy_rule
          - detector
          title: Source
          description: 'policy_rule: this backend''s safety policy; detector: POST .../detections.'
          default: policy_rule
        category:
          anyOf:
          - type: string
            enum:
            - safety
            - machine_health
            - fuel_energy
            - productivity
            - operator_behavior
            - security
            - planning
            - sensor_health
            - site
          - type: 'null'
          title: Category
          description: Detector alerts only.
        intelligence_level:
          anyOf:
          - type: string
            enum:
            - L1_threshold
            - L2_trend
            - L3_ml_pattern
            - L4_predictive
            - L5_contextual
          - type: 'null'
          title: Intelligence Level
          description: Detector alerts only.
        severity:
          type: string
          enum:
          - warning
          - critical
          title: Severity
        status:
          type: string
          enum:
          - active
          - cleared
          title: Status
        message:
          type: string
          title: Message
        explanation:
          type: string
          title: Explanation
        simulated:
          type: boolean
          title: Simulated
        trigger_readings:
          $ref: '#/components/schemas/TelemetryReadings'
        started_at:
          type: string
          format: date-time
          title: Started At
        cleared_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Cleared At
        policy_version:
          anyOf:
          - type: string
          - type: 'null'
          title: Policy Version
          description: Safety policy that produced this episode.
        source_status:
          anyOf:
          - type: string
            enum:
            - demo_assumption
            - published_limit
          - type: 'null'
          title: Source Status
          description: 'demo_assumption: thresholds are demo values, not published CAT limits.'
        reason:
          anyOf:
          - type: string
          - type: 'null'
          title: Reason
        recommended_action:
          anyOf:
          - type: string
          - type: 'null'
          title: Recommended Action
        evidence:
          anyOf:
          - $ref: '#/components/schemas/AlertEvidence'
          - type: 'null'
        correlated_alert_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Correlated Alert Id
          description: Episode this one overlaps (same physical situation); it is not announced again.
        draft_incident_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Draft Incident Id
          description: Automatic incident draft linked to this episode.
        training_assignment_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Training Assignment Id
          description: Lesson assignment linked to this episode.
        announced:
          type: boolean
          title: Announced
          description: 'An alert_started announcement was created (not: heard).'
          default: true
      additionalProperties: false
      type: object
      required:
      - alert_id
      - rule_id
      - alert_type
      - severity
      - status
      - message
      - explanation
      - simulated
      - trigger_readings
      - started_at
      title: Alert
    AlertEvidence:
      properties:
        event_id:
          type: string
          title: Event Id
        observed_at:
          type: string
          format: date-time
          title: Observed At
          description: Observation (data) time of the triggering sample.
        readings:
          $ref: '#/components/schemas/TelemetryReadings'
        idle_since:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Idle Since
          description: Start of the observed idle streak, if any.
        idle_seconds_observed:
          anyOf:
          - type: integer
          - type: 'null'
          title: Idle Seconds Observed
          description: observed_at - idle_since (data clock).
        machine_category:
          anyOf:
          - type: string
          - type: 'null'
          title: Machine Category
        applicability:
          type: string
          enum:
          - all_categories
          - category_listed
          title: Applicability
          default: all_categories
        detection:
          anyOf:
          - $ref: '#/components/schemas/DetectionSnapshot'
          - type: 'null'
          description: 'Present for detector alerts: the detection that opened the episode. `readings` is then the
            machine state at that moment.'
      additionalProperties: false
      type: object
      required:
      - event_id
      - observed_at
      - readings
      title: AlertEvidence
      description: What the rule saw when the episode opened. Saved once; later readings never change it.
    AlertExplainedAction:
      properties:
        type:
          type: string
          const: alert_explained
          title: Type
        alert:
          anyOf:
          - $ref: '#/components/schemas/Alert'
          - type: 'null'
        announcement_event_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Announcement Event Id
          description: The announcement that raised this alert.
        deliveries:
          items:
            $ref: '#/components/schemas/DeliveryRecord'
          type: array
          title: Deliveries
          description: Playback reports for that announcement. Played is not acknowledgement.
      additionalProperties: false
      type: object
      required:
      - type
      - alert
      title: AlertExplainedAction
      description: The explanation comes from the episode's saved evidence, never from the latest readings.
    Announcement:
      properties:
        event_id:
          type: string
          title: Event Id
        sequence:
          type: integer
          title: Sequence
        type:
          type: string
          enum:
          - alert_started
          - alert_cleared
          - shift_briefing
          title: Type
        priority:
          type: string
          enum:
          - low
          - normal
          - high
          - critical
          title: Priority
        speech:
          type: string
          title: Speech
        alert_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Alert Id
        created_at:
          type: string
          format: date-time
          title: Created At
        expires_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Expires At
        deliveries:
          items:
            $ref: '#/components/schemas/DeliveryRecord'
          type: array
          title: Deliveries
          description: Latest playback report per consumer. Played is not acknowledgement.
      additionalProperties: false
      type: object
      required:
      - event_id
      - sequence
      - type
      - priority
      - speech
      - created_at
      title: Announcement
    ApprovalRequest:
      properties:
        approval_id:
          type: string
          title: Approval Id
        kind:
          type: string
          const: incident_escalation
          title: Kind
        incident_id:
          type: string
          title: Incident Id
        status:
          type: string
          enum:
          - pending
          - approved
          - rejected
          - expired
          title: Status
        created_at:
          type: string
          format: date-time
          title: Created At
      additionalProperties: false
      type: object
      required:
      - approval_id
      - kind
      - incident_id
      - status
      - created_at
      title: ApprovalRequest
      description: 'A request for supervisor review. `pending` until a supervisor decision route exists (Batch D);
        creating it

        is not a notification and not an approval.'
    AssignedTask:
      properties:
        task_id:
          type: string
          title: Task Id
        shift_id:
          type: string
          title: Shift Id
        machine_id:
          type: string
          title: Machine Id
        site_zone_id:
          type: string
          title: Site Zone Id
        zone_name:
          type: string
          title: Zone Name
        scheduled_order:
          type: integer
          title: Scheduled Order
        scheduled_start_at:
          type: string
          format: date-time
          title: Scheduled Start At
        scheduled_start_local:
          type: string
          title: Scheduled Start Local
          description: HH:MM at the site (site UTC offset).
        task_type:
          type: string
          title: Task Type
        title:
          type: string
          title: Title
        details:
          type: string
          title: Details
        work_quantity:
          anyOf:
          - type: number
          - type: 'null'
          title: Work Quantity
        work_unit:
          anyOf:
          - type: string
          - type: 'null'
          title: Work Unit
        status:
          type: string
          enum:
          - scheduled
          - in_progress
          - completed
          title: Status
        version:
          type: integer
          title: Version
        started_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Started At
        completed_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Completed At
        weather:
          $ref: '#/components/schemas/TaskConditions'
        duration:
          $ref: '#/components/schemas/TaskDuration'
      additionalProperties: false
      type: object
      required:
      - task_id
      - shift_id
      - machine_id
      - site_zone_id
      - zone_name
      - scheduled_order
      - scheduled_start_at
      - scheduled_start_local
      - task_type
      - title
      - details
      - status
      - version
      - weather
      - duration
      title: AssignedTask
      description: A task assigned to the session's operator/machine for its trusted shift. Versioned for command
        concurrency.
    AssignedTasksAction:
      properties:
        type:
          type: string
          const: assigned_tasks
          title: Type
        scope:
          type: string
          enum:
          - next
          - all
          title: Scope
        tasks:
          items:
            $ref: '#/components/schemas/AssignedTask'
          type: array
          title: Tasks
        shift_bound:
          type: boolean
          title: Shift Bound
          description: False when the session has no trusted shift; tasks is then empty.
      additionalProperties: false
      type: object
      required:
      - type
      - scope
      - tasks
      - shift_bound
      title: AssignedTasksAction
      description: Read of the shift's assigned tasks (next task or full list).
    CapabilityUnavailableAction:
      properties:
        type:
          type: string
          const: capability_unavailable
          title: Type
        capability:
          type: string
          maxLength: 60
          title: Capability
      additionalProperties: false
      type: object
      required:
      - type
      - capability
      title: CapabilityUnavailableAction
    ClarificationAction:
      properties:
        type:
          type: string
          const: clarification_needed
          title: Type
        for_action:
          type: string
          enum:
          - confirm_draft
          - dismiss_draft
          - explain_alert
          - affirm
          - read_lesson
          title: For Action
        reason:
          type: string
          enum:
          - several_candidates
          - nothing_pending
          title: Reason
        options:
          items:
            type: string
          type: array
          title: Options
      additionalProperties: false
      type: object
      required:
      - type
      - for_action
      - reason
      title: ClarificationAction
      description: 'Nothing was changed: several workflows could match, or there is nothing to act on.'
    CommandPayload:
      properties:
        task_id:
          anyOf:
          - type: string
            pattern: ^[A-Za-z0-9][A-Za-z0-9._:\-]{0,127}$
            examples:
            - lk-item_4f2c9a
          - type: 'null'
          title: Task Id
        incident_id:
          anyOf:
          - type: string
            pattern: ^[A-Za-z0-9][A-Za-z0-9._:\-]{0,127}$
            examples:
            - lk-item_4f2c9a
          - type: 'null'
          title: Incident Id
          description: 'incident.* commands: the draft ID (DRF-…).'
        description:
          anyOf:
          - type: string
            maxLength: 1000
            minLength: 1
          - type: 'null'
          title: Description
        severity:
          anyOf:
          - type: string
            enum:
            - low
            - medium
            - high
            - critical
          - type: 'null'
          title: Severity
        location_text:
          anyOf:
          - type: string
            maxLength: 300
          - type: 'null'
          title: Location Text
      additionalProperties: false
      type: object
      title: CommandPayload
    DeliveryRecord:
      properties:
        event_id:
          type: string
          title: Event Id
        consumer_id:
          type: string
          title: Consumer Id
        status:
          type: string
          enum:
          - played
          - interrupted
          - failed
          - expired
          title: Status
        detail:
          anyOf:
          - type: string
          - type: 'null'
          title: Detail
        recorded_at:
          type: string
          format: date-time
          title: Recorded At
      additionalProperties: false
      type: object
      required:
      - event_id
      - consumer_id
      - status
      - recorded_at
      title: DeliveryRecord
    DeliveryReport:
      properties:
        consumer_id:
          type: string
          pattern: ^[A-Za-z0-9][A-Za-z0-9._:\-]{0,127}$
          title: Consumer Id
          examples:
          - lk-item_4f2c9a
        status:
          type: string
          enum:
          - played
          - interrupted
          - failed
          - expired
          title: Status
        detail:
          anyOf:
          - type: string
            maxLength: 500
          - type: 'null'
          title: Detail
      additionalProperties: false
      type: object
      required:
      - consumer_id
      - status
      title: DeliveryReport
    DetectionPrediction:
      properties:
        kind:
          type: string
          maxLength: 40
          title: Kind
          description: e.g. time_to_threshold, fuel_runout, task_finish, rain_start.
        eta:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Eta
        minutes_to_impact:
          anyOf:
          - type: number
          - type: 'null'
          title: Minutes To Impact
        value:
          anyOf:
          - type: number
          - type: 'null'
          title: Value
        unit:
          anyOf:
          - type: string
            maxLength: 20
          - type: 'null'
          title: Unit
      additionalProperties: false
      type: object
      required:
      - kind
      title: DetectionPrediction
    DetectionProvenance:
      properties:
        generator:
          type: string
          maxLength: 80
          title: Generator
          description: Detector that produced this detection, with its version.
        source_event_id:
          anyOf:
          - type: string
            maxLength: 120
          - type: 'null'
          title: Source Event Id
          description: The detector's own event ID.
      additionalProperties: false
      type: object
      required:
      - generator
      title: DetectionProvenance
    DetectionRequest:
      properties:
        detection_id:
          type: string
          pattern: ^[A-Za-z0-9][A-Za-z0-9._:\-]{0,127}$
          title: Detection Id
          description: Idempotency key within the session.
          examples:
          - lk-item_4f2c9a
        observed_at:
          type: string
          format: date-time
          title: Observed At
          description: Observation time the detector saw it; normalised to UTC.
        simulated:
          type: boolean
          const: true
          title: Simulated
          description: 'Must be true: detections come from simulated telemetry only.'
        episode_key:
          type: string
          pattern: ^[A-Za-z0-9][A-Za-z0-9._:\-]{0,127}$
          title: Episode Key
          description: Stable across one episode's lifecycle; one active alert per key.
          examples:
          - lk-item_4f2c9a
        status:
          type: string
          enum:
          - open
          - escalated
          - update
          - resolved
          title: Status
        detection_type:
          type: string
          pattern: ^[A-Z][A-Z0-9_]{1,63}$
          title: Detection Type
          examples:
          - PROXIMITY_DANGER_ZONE
        category:
          type: string
          enum:
          - safety
          - machine_health
          - fuel_energy
          - productivity
          - operator_behavior
          - security
          - planning
          - sensor_health
          - site
          title: Category
        intelligence_level:
          type: string
          enum:
          - L1_threshold
          - L2_trend
          - L3_ml_pattern
          - L4_predictive
          - L5_contextual
          title: Intelligence Level
        severity:
          type: string
          enum:
          - info
          - warning
          - critical
          title: Severity
          description: info is recorded but never opens an alert or an announcement.
        title:
          type: string
          maxLength: 120
          minLength: 1
          title: Title
        summary:
          type: string
          maxLength: 600
          minLength: 1
          title: Summary
          description: Deterministic sentence with the key numbers.
        speech:
          anyOf:
          - type: string
            maxLength: 300
          - type: 'null'
          title: Speech
          description: 'Operator-facing announcement; default: title plus the first action.'
        recommended_actions:
          items:
            type: string
            maxLength: 200
          type: array
          maxItems: 5
          title: Recommended Actions
        evidence:
          additionalProperties:
            $ref: '#/components/schemas/DetectionSignal'
          type: object
          maxProperties: 20
          title: Evidence
        prediction:
          anyOf:
          - $ref: '#/components/schemas/DetectionPrediction'
          - type: 'null'
        training_module:
          anyOf:
          - $ref: '#/components/schemas/DetectionTraining'
          - type: 'null'
        provenance:
          $ref: '#/components/schemas/DetectionProvenance'
        one_shot:
          type: boolean
          title: One Shot
          description: 'A point-in-time notice (e.g. a hot shutdown) with no lifecycle: an announced episode is
            opened and closed at once, so it stays explainable but never active.'
          default: false
      additionalProperties: false
      type: object
      required:
      - detection_id
      - observed_at
      - simulated
      - episode_key
      - status
      - detection_type
      - category
      - intelligence_level
      - severity
      - title
      - summary
      - provenance
      title: DetectionRequest
      description: 'One lifecycle step of an alert episode found by an external, trusted detector (the simulator''s
        detection

        layer). The backend turns it into the same alert episode, announcement and "Why?" evidence as its own rules.'
    DetectionResult:
      properties:
        session_id:
          type: string
          title: Session Id
        detection_id:
          type: string
          title: Detection Id
        duplicate:
          type: boolean
          title: Duplicate
        outcome:
          type: string
          enum:
          - alert_opened
          - alert_escalated
          - alert_cleared
          - recorded
          title: Outcome
        ignored_reason:
          anyOf:
          - type: string
            enum:
            - below_announce_threshold
            - already_active
            - no_active_episode
            - not_higher
            - update_only
          - type: 'null'
          title: Ignored Reason
          description: Why a recorded detection changed no alert.
        alert_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Alert Id
          description: The episode this detection opened, escalated or cleared.
        alerts_opened:
          items:
            type: string
          type: array
          title: Alerts Opened
        alerts_cleared:
          items:
            type: string
          type: array
          title: Alerts Cleared
        announcements_created:
          items:
            type: string
          type: array
          title: Announcements Created
        drafts_created:
          items:
            type: string
          type: array
          title: Drafts Created
        active_alerts:
          items:
            $ref: '#/components/schemas/Alert'
          type: array
          title: Active Alerts
        state_version:
          type: integer
          title: State Version
      additionalProperties: false
      type: object
      required:
      - session_id
      - detection_id
      - duplicate
      - outcome
      - alerts_opened
      - alerts_cleared
      - announcements_created
      - active_alerts
      - state_version
      title: DetectionResult
    DetectionSignal:
      properties:
        value:
          anyOf:
          - type: number
          - type: integer
          - type: boolean
          - type: string
          - type: 'null'
          title: Value
        unit:
          anyOf:
          - type: string
            maxLength: 20
          - type: 'null'
          title: Unit
        threshold:
          anyOf:
          - type: number
          - type: 'null'
          title: Threshold
        expected:
          anyOf:
          - type: number
          - type: 'null'
          title: Expected
          description: Model-expected value for the current conditions.
        deviation_sigma:
          anyOf:
          - type: number
          - type: 'null'
          title: Deviation Sigma
          description: Deviation from the model, in standard errors.
        trend_per_min:
          anyOf:
          - type: number
          - type: 'null'
          title: Trend Per Min
        baseline:
          anyOf:
          - type: number
          - type: 'null'
          title: Baseline
          description: Operator or fleet baseline for comparison.
        spn:
          anyOf:
          - type: integer
          - type: 'null'
          title: Spn
          description: J1939 suspect parameter number, where one applies.
        note:
          anyOf:
          - type: string
            maxLength: 200
          - type: 'null'
          title: Note
      additionalProperties: false
      type: object
      required:
      - value
      title: DetectionSignal
      description: One piece of evidence behind a detection. Only `value` is always present.
    DetectionSnapshot:
      properties:
        detection_id:
          type: string
          title: Detection Id
        detection_type:
          type: string
          title: Detection Type
        category:
          type: string
          enum:
          - safety
          - machine_health
          - fuel_energy
          - productivity
          - operator_behavior
          - security
          - planning
          - sensor_health
          - site
          title: Category
        intelligence_level:
          type: string
          enum:
          - L1_threshold
          - L2_trend
          - L3_ml_pattern
          - L4_predictive
          - L5_contextual
          title: Intelligence Level
        title:
          type: string
          title: Title
        summary:
          type: string
          title: Summary
        signals:
          additionalProperties:
            $ref: '#/components/schemas/DetectionSignal'
          type: object
          title: Signals
        prediction:
          anyOf:
          - $ref: '#/components/schemas/DetectionPrediction'
          - type: 'null'
        training_module:
          anyOf:
          - $ref: '#/components/schemas/DetectionTraining'
          - type: 'null'
        recommended_actions:
          items:
            type: string
          type: array
          title: Recommended Actions
        generator:
          type: string
          title: Generator
      additionalProperties: false
      type: object
      required:
      - detection_id
      - detection_type
      - category
      - intelligence_level
      - title
      - summary
      - generator
      title: DetectionSnapshot
      description: The detection that opened an episode, saved once as the episode's evidence.
    DetectionTraining:
      properties:
        module_id:
          type: string
          maxLength: 20
          title: Module Id
        title:
          type: string
          maxLength: 120
          title: Title
      additionalProperties: false
      type: object
      required:
      - module_id
      - title
      title: DetectionTraining
    ErrorBody:
      properties:
        code:
          type: string
          enum:
          - unauthorized
          - not_found
          - validation_error
          - idempotency_conflict
          - session_conflict
          - llm_unavailable
          - turn_failed
          - internal_error
          - unknown_machine
          - unknown_operator
          - catalog_unavailable
          - forbidden
          - auth_unavailable
          - version_conflict
          - invalid_transition
          - machine_state_unavailable
          title: Code
        message:
          type: string
          title: Message
        retryable:
          type: boolean
          title: Retryable
        request_id:
          type: string
          title: Request Id
        details:
          anyOf:
          - items:
              $ref: '#/components/schemas/ErrorDetail'
            type: array
          - type: 'null'
          title: Details
      additionalProperties: false
      type: object
      required:
      - code
      - message
      - retryable
      - request_id
      title: ErrorBody
    ErrorDetail:
      properties:
        field:
          type: string
          title: Field
        issue:
          type: string
          title: Issue
      additionalProperties: false
      type: object
      required:
      - field
      - issue
      title: ErrorDetail
    ErrorResponse:
      properties:
        error:
          $ref: '#/components/schemas/ErrorBody'
      additionalProperties: false
      type: object
      required:
      - error
      title: ErrorResponse
    EscalationRequestedAction:
      properties:
        type:
          type: string
          const: escalation_requested
          title: Type
        approval:
          $ref: '#/components/schemas/ApprovalRequest'
        created:
          type: boolean
          title: Created
      additionalProperties: false
      type: object
      required:
      - type
      - approval
      - created
      title: EscalationRequestedAction
    EventsPage:
      properties:
        session_id:
          type: string
          title: Session Id
        events:
          items:
            $ref: '#/components/schemas/Announcement'
          type: array
          title: Events
        next_cursor:
          type: integer
          title: Next Cursor
          description: Pass as ?after= on the next poll.
        has_more:
          type: boolean
          title: Has More
      additionalProperties: false
      type: object
      required:
      - session_id
      - events
      - next_cursor
      - has_more
      title: EventsPage
    HealthResponse:
      properties:
        status:
          type: string
          const: ok
          title: Status
      additionalProperties: false
      type: object
      required:
      - status
      title: HealthResponse
    IdleReason:
      properties:
        reason_id:
          type: string
          title: Reason Id
        reason_text:
          type: string
          title: Reason Text
        alert_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Alert Id
        created_at:
          type: string
          format: date-time
          title: Created At
      additionalProperties: false
      type: object
      required:
      - reason_id
      - reason_text
      - created_at
      title: IdleReason
    IdleReasonRecordedAction:
      properties:
        type:
          type: string
          const: idle_reason_recorded
          title: Type
        reason_id:
          type: string
          title: Reason Id
        reason_text:
          type: string
          title: Reason Text
        alert_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Alert Id
          description: Idle episode the reason is linked to, if one is active.
        belt_warning_active:
          type: boolean
          title: Belt Warning Active
        created:
          type: boolean
          title: Created
      additionalProperties: false
      type: object
      required:
      - type
      - reason_id
      - reason_text
      - belt_warning_active
      - created
      title: IdleReasonRecordedAction
      description: The operator's stated reason for idling. Recording it does not clear any warning.
    Incident:
      properties:
        incident_id:
          type: string
          title: Incident Id
        incident_number:
          type: integer
          title: Incident Number
        session_id:
          type: string
          title: Session Id
        operator_id:
          type: string
          title: Operator Id
        machine_id:
          type: string
          title: Machine Id
        description:
          type: string
          title: Description
        source_turn_id:
          type: string
          title: Source Turn Id
        created_at:
          type: string
          format: date-time
          title: Created At
        status:
          type: string
          const: confirmed
          title: Status
          description: Unconfirmed drafts are IncidentDraft.
          default: confirmed
        origin:
          type: string
          enum:
          - operator_reported
          - auto_draft
          title: Origin
          default: operator_reported
        severity:
          anyOf:
          - type: string
            enum:
            - low
            - medium
            - high
            - critical
          - type: 'null'
          title: Severity
          description: Null = not stated (never guessed).
        severity_basis:
          anyOf:
          - type: string
            enum:
            - reported
            - rule_default
          - type: 'null'
          title: Severity Basis
        site_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Site Id
        site_zone_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Site Zone Id
        zone_basis:
          anyOf:
          - type: string
            enum:
            - reported
            - active_task
          - type: 'null'
          title: Zone Basis
        location_text:
          anyOf:
          - type: string
          - type: 'null'
          title: Location Text
        occurred_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Occurred At
        occurred_basis:
          anyOf:
          - type: string
            enum:
            - time_of_report
            - observation_time
          - type: 'null'
          title: Occurred Basis
        episode_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Episode Id
          description: Alert episode behind a confirmed automatic draft.
        draft_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Draft Id
          description: The draft this incident was confirmed from.
        confirmed_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Confirmed At
      additionalProperties: false
      type: object
      required:
      - incident_id
      - incident_number
      - session_id
      - operator_id
      - machine_id
      - description
      - source_turn_id
      - created_at
      title: Incident
      description: 'A confirmed report. `description` is the "what". Structured fields are additive; each `*_basis`
        says where a

        value came from (stated by the operator, taken from recorded context, or a rule default) so nothing looks

        invented.'
    IncidentDraft:
      properties:
        draft_id:
          type: string
          title: Draft Id
        draft_number:
          type: integer
          title: Draft Number
        session_id:
          type: string
          title: Session Id
        operator_id:
          type: string
          title: Operator Id
        machine_id:
          type: string
          title: Machine Id
        origin:
          type: string
          const: auto_draft
          title: Origin
        status:
          type: string
          enum:
          - draft
          - confirmed
          - dismissed
          title: Status
        description:
          type: string
          title: Description
        severity:
          anyOf:
          - type: string
            enum:
            - low
            - medium
            - high
            - critical
          - type: 'null'
          title: Severity
        severity_basis:
          anyOf:
          - type: string
            enum:
            - reported
            - rule_default
          - type: 'null'
          title: Severity Basis
        site_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Site Id
        site_zone_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Site Zone Id
        zone_basis:
          anyOf:
          - type: string
            enum:
            - reported
            - active_task
          - type: 'null'
          title: Zone Basis
        location_text:
          anyOf:
          - type: string
          - type: 'null'
          title: Location Text
        occurred_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Occurred At
        occurred_basis:
          anyOf:
          - type: string
            enum:
            - time_of_report
            - observation_time
          - type: 'null'
          title: Occurred Basis
        episode_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Episode Id
        version:
          type: integer
          title: Version
        incident_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Incident Id
          description: Set once, when the draft is confirmed.
        created_at:
          type: string
          format: date-time
          title: Created At
        confirmed_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Confirmed At
        dismissed_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Dismissed At
      additionalProperties: false
      type: object
      required:
      - draft_id
      - draft_number
      - session_id
      - operator_id
      - machine_id
      - origin
      - status
      - description
      - version
      - created_at
      title: IncidentDraft
      description: 'An automatic draft from a safety episode. Not an incident until confirmed; confirming allocates
        the real

        incident ID once (`incident_id`).'
    IncidentDraftAction:
      properties:
        type:
          type: string
          enum:
          - incident_confirmed
          - incident_dismissed
          title: Type
        draft:
          $ref: '#/components/schemas/IncidentDraft'
        incident:
          anyOf:
          - $ref: '#/components/schemas/Incident'
          - type: 'null'
          description: The incident created by confirming (confirm only).
        created:
          type: boolean
          title: Created
      additionalProperties: false
      type: object
      required:
      - type
      - draft
      - created
      title: IncidentDraftAction
    IncidentDraftsAction:
      properties:
        type:
          type: string
          const: incident_drafts
          title: Type
        drafts:
          items:
            $ref: '#/components/schemas/IncidentDraft'
          type: array
          title: Drafts
      additionalProperties: false
      type: object
      required:
      - type
      - drafts
      title: IncidentDraftsAction
    IncidentLoggedAction:
      properties:
        type:
          type: string
          const: incident_logged
          title: Type
        incident:
          $ref: '#/components/schemas/Incident'
        created:
          type: boolean
          title: Created
          description: False when an earlier attempt of the same turn already saved it.
      additionalProperties: false
      type: object
      required:
      - type
      - incident
      - created
      title: IncidentLoggedAction
    InformationRequestedAction:
      properties:
        type:
          type: string
          const: information_requested
          title: Type
        for_action:
          type: string
          const: log_incident
          title: For Action
        missing_field:
          type: string
          const: description
          title: Missing Field
      additionalProperties: false
      type: object
      required:
      - type
      - for_action
      - missing_field
      title: InformationRequestedAction
    Lesson:
      properties:
        lesson_id:
          type: string
          title: Lesson Id
        title:
          type: string
          title: Title
        summary:
          type: string
          title: Summary
        duration_minutes:
          type: integer
          title: Duration Minutes
        version:
          anyOf:
          - type: string
          - type: 'null'
          title: Version
          description: Content version, when lesson text exists.
        content_text:
          anyOf:
          - type: string
          - type: 'null'
          title: Content Text
          description: Readable lesson text (demo lessons only so far).
        content_status:
          anyOf:
          - type: string
            const: demo_authored_unreviewed
          - type: 'null'
          title: Content Status
          description: 'demo_authored_unreviewed: written for the demo, not reviewed by a trainer.'
      additionalProperties: false
      type: object
      required:
      - lesson_id
      - title
      - summary
      - duration_minutes
      title: Lesson
    LessonContentAction:
      properties:
        type:
          type: string
          const: lesson_content
          title: Type
        lesson:
          $ref: '#/components/schemas/Lesson'
        assignment:
          anyOf:
          - $ref: '#/components/schemas/TrainingAssignment'
          - type: 'null'
      additionalProperties: false
      type: object
      required:
      - type
      - lesson
      title: LessonContentAction
    MachineStateView:
      properties:
        status:
          type: string
          enum:
          - unavailable
          - fresh
          - stale
          title: Status
        observed_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Observed At
        received_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Received At
        engine_on:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Engine On
        seatbelt_fastened:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Seatbelt Fastened
        operating_state:
          anyOf:
          - type: string
            enum:
            - 'off'
            - idle
            - working
            - travel
          - type: 'null'
          title: Operating State
        speed_kph:
          anyOf:
          - type: number
          - type: 'null'
          title: Speed Kph
        idle_since:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Idle Since
        idle_seconds_observed:
          anyOf:
          - type: integer
          - type: 'null'
          title: Idle Seconds Observed
        stale_after_seconds:
          type: integer
          title: Stale After Seconds
      additionalProperties: false
      type: object
      required:
      - status
      - stale_after_seconds
      title: MachineStateView
      description: 'Latest accepted observation. Two clocks: durations use observation time (`observed_at`); freshness
        uses the

        server''s receipt time. Missing or stale data is reported as such, never as healthy.'
    MeResponse:
      properties:
        subject_id:
          type: string
          title: Subject Id
          description: '`service` for the service credential; otherwise the principal_id.'
        principal_kind:
          type: string
          enum:
          - service
          - operator
          - supervisor
          title: Principal Kind
        operator_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Operator Id
          description: Catalog operator ID of an operator principal.
        display_name:
          anyOf:
          - type: string
          - type: 'null'
          title: Display Name
        site_ids:
          items:
            type: string
          type: array
          title: Site Ids
          description: 'Granted sites. Always empty in I02b: no site grants exist yet.'
        allowed_associations:
          items:
            $ref: '#/components/schemas/SessionAssociation'
          type: array
          title: Allowed Associations
          description: 'Operator: own catalog_verified sessions. Service and supervisor: empty (the service is trusted
            for all sessions it manages; supervisors have no session access in I02b).'
        scopes:
          items:
            type: string
          type: array
          title: Scopes
          description: Actor token scopes; empty for the service.
        token_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Token Id
          description: Non-secret record ID of the presented actor token.
        token_expires_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Token Expires At
          description: Null for the service credential.
      additionalProperties: false
      type: object
      required:
      - subject_id
      - principal_kind
      - site_ids
      - allowed_associations
      title: MeResponse
      description: GET /v1/me. Never contains a bearer token, its digest or service configuration.
    NextTaskAction:
      properties:
        type:
          type: string
          const: next_task
          title: Type
        task:
          anyOf:
          - $ref: '#/components/schemas/Task'
          - type: 'null'
      additionalProperties: false
      type: object
      required:
      - type
      - task
      title: NextTaskAction
    PendingCancelledAction:
      properties:
        type:
          type: string
          const: pending_cancelled
          title: Type
        cancelled:
          anyOf:
          - type: string
            const: log_incident
          - type: 'null'
          title: Cancelled
      additionalProperties: false
      type: object
      required:
      - type
      - cancelled
      title: PendingCancelledAction
    PendingQuestion:
      properties:
        kind:
          type: string
          const: incident_description
          title: Kind
        for_action:
          type: string
          const: log_incident
          title: For Action
        asked_in_turn_id:
          type: string
          title: Asked In Turn Id
        notify_supervisor:
          type: boolean
          title: Notify Supervisor
          description: The report was asked to go to a supervisor too.
          default: false
        severity:
          anyOf:
          - type: string
            enum:
            - low
            - medium
            - high
            - critical
          - type: 'null'
          title: Severity
        location_text:
          anyOf:
          - type: string
          - type: 'null'
          title: Location Text
      additionalProperties: false
      type: object
      required:
      - kind
      - for_action
      - asked_in_turn_id
      title: PendingQuestion
    ReadyResponse:
      properties:
        status:
          type: string
          enum:
          - ready
          - not_ready
          title: Status
        llm_mode:
          type: string
          enum:
          - live
          - mock
          title: Llm Mode
        database:
          type: boolean
          title: Database
        checkpointer:
          type: boolean
          title: Checkpointer
        version:
          type: string
          title: Version
        catalog:
          type: boolean
          title: Catalog
          description: Verified machine/operator catalog loaded; needed to admit new sessions. Existing sessions
            keep working without it.
          default: false
        catalog_version:
          anyOf:
          - type: string
          - type: 'null'
          title: Catalog Version
          description: Manifest SHA-256 of the loaded catalog.
        catalog_issue:
          anyOf:
          - type: string
          - type: 'null'
          title: Catalog Issue
          description: Sanitized reason code when catalog is false.
        schema_version:
          anyOf:
          - type: integer
          - type: 'null'
          title: Schema Version
          description: Applied cocoon.db migration version.
      additionalProperties: false
      type: object
      required:
      - status
      - llm_mode
      - database
      - checkpointer
      - version
      title: ReadyResponse
    Session:
      properties:
        session_id:
          type: string
          title: Session Id
        client_session_key:
          type: string
          title: Client Session Key
        room_name:
          type: string
          title: Room Name
        participant_identity:
          type: string
          title: Participant Identity
        operator_id:
          type: string
          title: Operator Id
        machine_id:
          type: string
          title: Machine Id
        state_version:
          type: integer
          title: State Version
        created_at:
          type: string
          format: date-time
          title: Created At
        dataset_manifest_sha256:
          anyOf:
          - type: string
          - type: 'null'
          title: Dataset Manifest Sha256
          description: Verified catalog snapshot this session was admitted under. Null for sessions created before
            catalog binding existed (never attached retroactively).
        binding_status:
          type: string
          enum:
          - catalog_verified
          - legacy_unverified
          title: Binding Status
          description: 'catalog_verified: IDs checked against dataset_manifest_sha256 at creation. legacy_unverified:
            pre-upgrade session; its IDs were never checked.'
          default: legacy_unverified
        site_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Site Id
        shift_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Shift Id
        context_status:
          type: string
          enum:
          - unavailable
          - trusted_binding
          - legacy_unverified
          title: Context Status
          description: 'unavailable: no trusted site/shift is known (not a default site). trusted_binding: site/shift
            matched a server-configured binding. legacy_unverified: pre-upgrade session.'
          default: legacy_unverified
        context_source:
          anyOf:
          - type: string
          - type: 'null'
          title: Context Source
          description: Binding record that established site/shift.
      additionalProperties: false
      type: object
      required:
      - session_id
      - client_session_key
      - room_name
      - participant_identity
      - operator_id
      - machine_id
      - state_version
      - created_at
      title: Session
    SessionAssociation:
      properties:
        operator_id:
          type: string
          title: Operator Id
        machine_id:
          type: string
          title: Machine Id
        site_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Site Id
        shift_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Shift Id
        session_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Session Id
          description: The owned session this association comes from.
      additionalProperties: false
      type: object
      required:
      - operator_id
      - machine_id
      title: SessionAssociation
      description: 'An association the caller actually holds. For an operator: one of their own catalog_verified
        sessions,

        created by the trusted service. Catalog membership alone is never listed as an association.'
    SessionCommand:
      properties:
        schema_version:
          type: string
          const: cocoon.command.v1
          title: Schema Version
          default: cocoon.command.v1
        command_id:
          type: string
          pattern: ^[A-Za-z0-9][A-Za-z0-9._:\-]{0,127}$
          title: Command Id
          description: Unique per caller; an identical retry returns the saved result.
          examples:
          - lk-item_4f2c9a
        kind:
          type: string
          enum:
          - task.start
          - task.complete
          - incident.edit
          - incident.confirm
          - incident.dismiss
          title: Kind
        captured_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Captured At
          description: Device time of the tap (informational).
        expected_version:
          anyOf:
          - type: integer
            minimum: 1.0
          - type: 'null'
          title: Expected Version
        payload:
          $ref: '#/components/schemas/CommandPayload'
      additionalProperties: false
      type: object
      required:
      - command_id
      - kind
      - payload
      title: SessionCommand
      description: 'POST /v1/sessions/{session_id}/commands. A subset of the proposed cocoon.command.v1 envelope:
        the binding is

        taken from the authenticated session (never from the body); `expected_version` guards against stale taps.'
    SessionCommandResult:
      properties:
        command_id:
          type: string
          title: Command Id
        kind:
          type: string
          title: Kind
        status:
          type: string
          const: completed
          title: Status
        duplicate:
          type: boolean
          title: Duplicate
          description: True when this identical command was already committed.
        record_type:
          anyOf:
          - type: string
          - type: 'null'
          title: Record Type
        record_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Record Id
        summary:
          type: string
          title: Summary
        state_version:
          type: integer
          title: State Version
        task:
          anyOf:
          - $ref: '#/components/schemas/AssignedTask'
          - type: 'null'
        draft:
          anyOf:
          - $ref: '#/components/schemas/IncidentDraft'
          - type: 'null'
        incident:
          anyOf:
          - $ref: '#/components/schemas/Incident'
          - type: 'null'
        created_at:
          type: string
          format: date-time
          title: Created At
      additionalProperties: false
      type: object
      required:
      - command_id
      - kind
      - status
      - duplicate
      - summary
      - state_version
      - created_at
      title: SessionCommandResult
    SessionCreateRequest:
      properties:
        client_session_key:
          type: string
          maxLength: 256
          minLength: 1
          title: Client Session Key
        room_name:
          type: string
          maxLength: 256
          minLength: 1
          title: Room Name
        participant_identity:
          type: string
          maxLength: 256
          minLength: 1
          title: Participant Identity
        operator_id:
          type: string
          maxLength: 128
          minLength: 1
          title: Operator Id
        machine_id:
          type: string
          maxLength: 128
          minLength: 1
          title: Machine Id
          description: Exact catalog asset ID for a NEW session (unknown → 422 unknown_machine). An existing client_session_key
            is resolved against its stored association first.
        site_id:
          anyOf:
          - type: string
            pattern: ^[A-Za-z0-9][A-Za-z0-9._:\-]{0,127}$
          - type: 'null'
          title: Site Id
          description: Optional. Accepted only together with shift_id and only if it matches a server-configured
            trusted binding; otherwise 422. Never authoritative on its own.
        shift_id:
          anyOf:
          - type: string
            pattern: ^[A-Za-z0-9][A-Za-z0-9._:\-]{0,127}$
          - type: 'null'
          title: Shift Id
          description: Optional; see site_id.
      additionalProperties: false
      type: object
      required:
      - client_session_key
      - room_name
      - participant_identity
      - operator_id
      - machine_id
      title: SessionCreateRequest
    SessionState:
      properties:
        session_id:
          type: string
          title: Session Id
        state_version:
          type: integer
          title: State Version
        llm_mode:
          type: string
          enum:
          - live
          - mock
          title: Llm Mode
        tasks:
          items:
            $ref: '#/components/schemas/Task'
          type: array
          title: Tasks
        incidents:
          items:
            $ref: '#/components/schemas/Incident'
          type: array
          title: Incidents
        training_assignments:
          items:
            $ref: '#/components/schemas/TrainingAssignment'
          type: array
          title: Training Assignments
        available_lessons:
          items:
            $ref: '#/components/schemas/Lesson'
          type: array
          title: Available Lessons
        active_alerts:
          items:
            $ref: '#/components/schemas/Alert'
          type: array
          title: Active Alerts
        latest_alert:
          anyOf:
          - $ref: '#/components/schemas/Alert'
          - type: 'null'
        pending_question:
          anyOf:
          - $ref: '#/components/schemas/PendingQuestion'
          - type: 'null'
        shift:
          anyOf:
          - $ref: '#/components/schemas/ShiftInfo'
          - type: 'null'
          description: The session's trusted shift (synthetic demo fixture), or null when unbound.
        assigned_tasks:
          items:
            $ref: '#/components/schemas/AssignedTask'
          type: array
          title: Assigned Tasks
          description: Tasks of the trusted shift. `tasks` stays the legacy shared demo list.
        incident_drafts:
          items:
            $ref: '#/components/schemas/IncidentDraft'
          type: array
          title: Incident Drafts
          description: Open automatic drafts. `incidents` lists confirmed reports only.
        pending_approvals:
          items:
            $ref: '#/components/schemas/ApprovalRequest'
          type: array
          title: Pending Approvals
        machine_state:
          anyOf:
          - $ref: '#/components/schemas/MachineStateView'
          - type: 'null'
          description: Freshness of machine observations.
        idle_reasons:
          items:
            $ref: '#/components/schemas/IdleReason'
          type: array
          title: Idle Reasons
        shift_briefing:
          anyOf:
          - $ref: '#/components/schemas/ShiftBriefing'
          - type: 'null'
      additionalProperties: false
      type: object
      required:
      - session_id
      - state_version
      - llm_mode
      - tasks
      - incidents
      - training_assignments
      - available_lessons
      - active_alerts
      - latest_alert
      - pending_question
      title: SessionState
    ShiftBriefing:
      properties:
        shift_id:
          type: string
          title: Shift Id
        event_id:
          type: string
          title: Event Id
        speech:
          type: string
          title: Speech
        created_at:
          type: string
          format: date-time
          title: Created At
      additionalProperties: false
      type: object
      required:
      - shift_id
      - event_id
      - speech
      - created_at
      title: ShiftBriefing
      description: Created once per shift and published as a `shift_briefing` announcement.
    ShiftInfo:
      properties:
        shift_id:
          type: string
          title: Shift Id
        site_id:
          type: string
          title: Site Id
        site_name:
          type: string
          title: Site Name
        timezone:
          type: string
          title: Timezone
        service_date:
          type: string
          title: Service Date
        start_at:
          type: string
          format: date-time
          title: Start At
        end_at:
          type: string
          format: date-time
          title: End At
        utc_offset:
          type: string
          pattern: ^[+-]\d{2}:\d{2}$
          title: Utc Offset
          description: Site offset used for the service date.
        source:
          type: string
          const: synthetic_demo_fixture
          title: Source
      additionalProperties: false
      type: object
      required:
      - shift_id
      - site_id
      - site_name
      - timezone
      - service_date
      - start_at
      - end_at
      - utc_offset
      - source
      title: ShiftInfo
    Task:
      properties:
        task_id:
          type: string
          title: Task Id
        title:
          type: string
          title: Title
        details:
          type: string
          title: Details
        priority:
          type: string
          enum:
          - low
          - normal
          - high
          title: Priority
        status:
          type: string
          enum:
          - pending
          - in_progress
          - done
          title: Status
      additionalProperties: false
      type: object
      required:
      - task_id
      - title
      - details
      - priority
      - status
      title: Task
    TaskConditions:
      properties:
        source:
          type: string
          const: synthetic_demo_fixture
          title: Source
          description: Where the conditions come from. Only a synthetic fixture exists until live weather (Batch
            C).
        summary:
          anyOf:
          - type: string
          - type: 'null'
          title: Summary
        temperature_c:
          anyOf:
          - type: number
          - type: 'null'
          title: Temperature C
      additionalProperties: false
      type: object
      required:
      - source
      title: TaskConditions
    TaskDuration:
      properties:
        minutes:
          anyOf:
          - type: integer
          - type: 'null'
          title: Minutes
        source:
          type: string
          const: demo_supplied_estimate
          title: Source
          description: A supplied demo figure, not a calibrated prediction (the estimator is Batch C).
      additionalProperties: false
      type: object
      required:
      - source
      title: TaskDuration
    TaskRejectedAction:
      properties:
        type:
          type: string
          const: task_rejected
          title: Type
        for_action:
          type: string
          enum:
          - task.start
          - task.complete
          title: For Action
        reason:
          type: string
          enum:
          - no_shift
          - no_eligible_task
          - invalid_transition
          title: Reason
        current_status:
          anyOf:
          - type: string
          - type: 'null'
          title: Current Status
      additionalProperties: false
      type: object
      required:
      - type
      - for_action
      - reason
      title: TaskRejectedAction
      description: A task command that changed nothing, with the reason (no shift, no eligible task, illegal transition).
    TaskTransitionAction:
      properties:
        type:
          type: string
          enum:
          - task_started
          - task_completed
          title: Type
        task:
          $ref: '#/components/schemas/AssignedTask'
        command_id:
          type: string
          title: Command Id
        created:
          type: boolean
          title: Created
          description: False when this exact command was already committed (retry).
      additionalProperties: false
      type: object
      required:
      - type
      - task
      - command_id
      - created
      title: TaskTransitionAction
    TelemetryProvenance:
      properties:
        origin:
          type: string
          enum:
          - synthetic_scenario
          - dataset_replay
          title: Origin
        generator:
          type: string
          maxLength: 80
          title: Generator
        record_ref:
          anyOf:
          - type: string
            maxLength: 120
          - type: 'null'
          title: Record Ref
          description: Dataset record ID for a replay.
      additionalProperties: false
      type: object
      required:
      - origin
      - generator
      title: TelemetryProvenance
    TelemetryReadings:
      properties:
        engine_on:
          type: boolean
          title: Engine On
        seatbelt_fastened:
          type: boolean
          title: Seatbelt Fastened
        idle_seconds:
          type: integer
          maximum: 86400.0
          minimum: 0.0
          title: Idle Seconds
          description: Client-reported counter; informational. Idle rules time idling from observation timestamps
            instead.
        operating_state:
          anyOf:
          - type: string
            enum:
            - 'off'
            - idle
            - working
            - travel
          - type: 'null'
          title: Operating State
          description: 'Observed machine state. Omitted = unknown: idle rules neither open nor clear.'
        speed_kph:
          anyOf:
          - type: number
            maximum: 100.0
            minimum: 0.0
          - type: 'null'
          title: Speed Kph
          description: Observed ground speed (motion).
        seat_occupied:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Seat Occupied
          description: 'Operator seat switch. false = nobody in the seat, so seatbelt rules do not apply (an unattended
            machine is not an unbelted operator). Omitted = unknown: rules behave as before.'
      additionalProperties: false
      type: object
      required:
      - engine_on
      - seatbelt_fastened
      - idle_seconds
      title: TelemetryReadings
    TelemetryRequest:
      properties:
        event_id:
          type: string
          pattern: ^[A-Za-z0-9][A-Za-z0-9._:\-]{0,127}$
          title: Event Id
          examples:
          - lk-item_4f2c9a
        observed_at:
          type: string
          format: date-time
          title: Observed At
          description: Timezone-aware timestamp; normalised to UTC.
        simulated:
          type: boolean
          const: true
          title: Simulated
          description: 'Must be true: v1 accepts prototype simulator data only.'
        readings:
          $ref: '#/components/schemas/TelemetryReadings'
        provenance:
          anyOf:
          - $ref: '#/components/schemas/TelemetryProvenance'
          - type: 'null'
          description: Where a simulated sample came from. Stored, never used by the rules.
      additionalProperties: false
      type: object
      required:
      - event_id
      - observed_at
      - simulated
      - readings
      title: TelemetryRequest
    TelemetryResult:
      properties:
        session_id:
          type: string
          title: Session Id
        event_id:
          type: string
          title: Event Id
        duplicate:
          type: boolean
          title: Duplicate
        stale:
          type: boolean
          title: Stale
          description: True when the sample was not applied (late or conflicting).
        ignored_reason:
          anyOf:
          - type: string
            enum:
            - late
            - conflicting
          - type: 'null'
          title: Ignored Reason
          description: 'late: older than the newest applied sample; conflicting: same observation time as the newest
            applied sample but different readings.'
        alerts_opened:
          items:
            type: string
          type: array
          title: Alerts Opened
        alerts_cleared:
          items:
            type: string
          type: array
          title: Alerts Cleared
        announcements_created:
          items:
            type: string
          type: array
          title: Announcements Created
        drafts_created:
          items:
            type: string
          type: array
          title: Drafts Created
          description: Automatic incident drafts opened.
        active_alerts:
          items:
            $ref: '#/components/schemas/Alert'
          type: array
          title: Active Alerts
        state_version:
          type: integer
          title: State Version
      additionalProperties: false
      type: object
      required:
      - session_id
      - event_id
      - duplicate
      - stale
      - alerts_opened
      - alerts_cleared
      - announcements_created
      - active_alerts
      - state_version
      title: TelemetryResult
    TrainingAssignedAction:
      properties:
        type:
          type: string
          const: training_assigned
          title: Type
        assignment:
          $ref: '#/components/schemas/TrainingAssignment'
        created:
          type: boolean
          title: Created
          description: False when this lesson was already assigned to the operator.
      additionalProperties: false
      type: object
      required:
      - type
      - assignment
      - created
      title: TrainingAssignedAction
    TrainingAssignment:
      properties:
        assignment_id:
          type: string
          title: Assignment Id
        lesson_id:
          type: string
          title: Lesson Id
        lesson_title:
          type: string
          title: Lesson Title
        operator_id:
          type: string
          title: Operator Id
        status:
          type: string
          enum:
          - assigned
          - completed
          title: Status
          description: Reading or playback never sets completed.
        assigned_at:
          type: string
          format: date-time
          title: Assigned At
        source_episode_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Source Episode Id
          description: Safety episode that triggered the assignment.
      additionalProperties: false
      type: object
      required:
      - assignment_id
      - lesson_id
      - lesson_title
      - operator_id
      - status
      - assigned_at
      title: TrainingAssignment
    TrainingStatusAction:
      properties:
        type:
          type: string
          const: training_status
          title: Type
        assignments:
          items:
            $ref: '#/components/schemas/TrainingAssignment'
          type: array
          title: Assignments
        available_lessons:
          items:
            $ref: '#/components/schemas/Lesson'
          type: array
          title: Available Lessons
      additionalProperties: false
      type: object
      required:
      - type
      - assignments
      - available_lessons
      title: TrainingStatusAction
    TurnRequest:
      properties:
        turn_id:
          type: string
          pattern: ^[A-Za-z0-9][A-Za-z0-9._:\-]{0,127}$
          title: Turn Id
          examples:
          - lk-item_4f2c9a
        text:
          type: string
          maxLength: 2000
          minLength: 1
          title: Text
        source:
          type: string
          enum:
          - voice
          - text
          title: Source
      additionalProperties: false
      type: object
      required:
      - turn_id
      - text
      - source
      title: TurnRequest
    TurnResult:
      properties:
        session_id:
          type: string
          title: Session Id
        turn_id:
          type: string
          title: Turn Id
        status:
          type: string
          enum:
          - processing
          - completed
          - failed
          title: Status
        speech:
          anyOf:
          - type: string
          - type: 'null'
          title: Speech
          description: Operator-facing text for TTS. Present only when status is completed.
        actions:
          items:
            oneOf:
            - $ref: '#/components/schemas/NextTaskAction'
            - $ref: '#/components/schemas/IncidentLoggedAction'
            - $ref: '#/components/schemas/InformationRequestedAction'
            - $ref: '#/components/schemas/TrainingAssignedAction'
            - $ref: '#/components/schemas/TrainingStatusAction'
            - $ref: '#/components/schemas/AlertExplainedAction'
            - $ref: '#/components/schemas/PendingCancelledAction'
            - $ref: '#/components/schemas/AssignedTasksAction'
            - $ref: '#/components/schemas/TaskTransitionAction'
            - $ref: '#/components/schemas/TaskRejectedAction'
            - $ref: '#/components/schemas/IncidentDraftAction'
            - $ref: '#/components/schemas/IncidentDraftsAction'
            - $ref: '#/components/schemas/IdleReasonRecordedAction'
            - $ref: '#/components/schemas/LessonContentAction'
            - $ref: '#/components/schemas/EscalationRequestedAction'
            - $ref: '#/components/schemas/ClarificationAction'
            - $ref: '#/components/schemas/CapabilityUnavailableAction'
            discriminator:
              propertyName: type
              mapping:
                alert_explained: '#/components/schemas/AlertExplainedAction'
                assigned_tasks: '#/components/schemas/AssignedTasksAction'
                capability_unavailable: '#/components/schemas/CapabilityUnavailableAction'
                clarification_needed: '#/components/schemas/ClarificationAction'
                escalation_requested: '#/components/schemas/EscalationRequestedAction'
                idle_reason_recorded: '#/components/schemas/IdleReasonRecordedAction'
                incident_confirmed: '#/components/schemas/IncidentDraftAction'
                incident_dismissed: '#/components/schemas/IncidentDraftAction'
                incident_drafts: '#/components/schemas/IncidentDraftsAction'
                incident_logged: '#/components/schemas/IncidentLoggedAction'
                information_requested: '#/components/schemas/InformationRequestedAction'
                lesson_content: '#/components/schemas/LessonContentAction'
                next_task: '#/components/schemas/NextTaskAction'
                pending_cancelled: '#/components/schemas/PendingCancelledAction'
                task_completed: '#/components/schemas/TaskTransitionAction'
                task_rejected: '#/components/schemas/TaskRejectedAction'
                task_started: '#/components/schemas/TaskTransitionAction'
                training_assigned: '#/components/schemas/TrainingAssignedAction'
                training_status: '#/components/schemas/TrainingStatusAction'
          type: array
          title: Actions
        state_version:
          anyOf:
          - type: integer
          - type: 'null'
          title: State Version
        llm_mode:
          anyOf:
          - type: string
            enum:
            - live
            - mock
          - type: 'null'
          title: Llm Mode
        error:
          anyOf:
          - $ref: '#/components/schemas/ErrorBody'
          - type: 'null'
        retry_after_ms:
          anyOf:
          - type: integer
          - type: 'null'
          title: Retry After Ms
          description: 'Present when status is processing: wait this long, then GET poll_url.'
        poll_url:
          anyOf:
          - type: string
          - type: 'null'
          title: Poll Url
        created_at:
          type: string
          format: date-time
          title: Created At
        completed_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Completed At
        branch:
          anyOf:
          - type: string
            enum:
            - tasks
            - safety_incidents
            - training
            - general_assistance
          - type: 'null'
          title: Branch
          description: Workflow branch the turn was classified into.
        action_records:
          items:
            $ref: '#/components/schemas/ActionRecord'
          type: array
          title: Action Records
          description: 'Writes committed by this turn, also on a failed turn: a failure after a committed write
            never means nothing was saved.'
      additionalProperties: false
      type: object
      required:
      - session_id
      - turn_id
      - status
      - created_at
      title: TurnResult
  securitySchemes:
    HTTPBearer:
      type: http
      description: 'Exactly ONE of: the trusted service credential (COCOON_SERVICE_TOKEN; server-side callers only,
        never embedded in Android or React), or an actor token (`cct_...`) issued locally by scripts/actor_tokens.py
        for an operator or supervisor principal. Access per route is listed in API_CONTRACT.md.'
      scheme: bearer
